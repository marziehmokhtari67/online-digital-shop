import { createTheme } from "@mui/material";

 export const theme = createTheme({
    typography: {
      fontFamily: 
        "Vazir-Medium",
        flexGrow: 1,
        fontSize:14,
        
    },
    tableCell:{
        fontFamily: 
        "Vazir-Medium", 
    },
   
  });